import { LinkHref } from '../../shared';

export const Footer = () => {
    return (
        <div>
            <LinkHref label="Главная" />
            <LinkHref label="О нас" />
        </div>
    );
};
