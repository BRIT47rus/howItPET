export interface Finance {
    titleData: string;
    bgImage: string;
    ipoteka: {
        title: string;
        steps: {
            title: string;
            text: string;
        }[];
    };
}
